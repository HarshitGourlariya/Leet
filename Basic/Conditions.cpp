#include<bits/stdc++.h>
using namespace std;

int main(){
    int x;
    cin >> x;
    if(x>18){   //If we have used an if statement it is not necessary to use the else statement 
        
        cout << "Adult";
    }
    else
    cout << "Not Adult";
  
    //Like this we can use "if else if 
    return 0;
}